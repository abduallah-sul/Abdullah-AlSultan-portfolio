<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ملف أعمال عبدالله السلطان</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #F5F1E3; /* لون الخلفية الكريمي من الملف */
            --primary-color: #7D6E5D; /* اللون البني الأساسي */
            --accent-color: #D9D2B5; /* لون ثانوي للدوائر والزخارف */
            --text-dark: #333;
            --white: #ffffff;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Cairo', sans-serif;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-dark);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* Hero Section */
        header {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            position: relative;
            background: linear-gradient(135deg, var(--bg-color) 60%, var(--accent-color) 100%);
            padding: 20px;
        }

        .hero-circle {
            position: absolute;
            width: 300px;
            height: 300px;
            border: 2px solid var(--primary-color);
            border-radius: 50%;
            top: -50px;
            right: -50px;
            opacity: 0.2;
        }

        header h1 {
            font-size: 3rem;
            color: var(--primary-color);
            margin-bottom: 10px;
        }

        header p {
            font-size: 1.2rem;
            max-width: 600px;
        }

        .btn {
            display: inline-block;
            margin-top: 25px;
            padding: 12px 30px;
            background-color: var(--primary-color);
            color: var(--white);
            text-decoration: none;
            border-radius: 25px;
            transition: 0.3s;
            font-weight: bold;
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        /* Sections Styling */
        section {
            padding: 80px 10%;
        }

        h2 {
            text-align: center;
            color: var(--primary-color);
            margin-bottom: 40px;
            position: relative;
        }

        h2::after {
            content: '';
            display: block;
            width: 50px;
            height: 3px;
            background: var(--primary-color);
            margin: 10px auto;
        }

        /* Projects Grid */
        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
        }

        .card {
            background: var(--white);
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
            transition: 0.3s;
            border-bottom: 4px solid transparent;
        }

        .card:hover {
            border-bottom: 4px solid var(--primary-color);
            transform: translateY(-5px);
        }

        .card h3 {
            color: var(--primary-color);
            margin-bottom: 15px;
        }

        /* Tools List */
        .tools-list {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 15px;
        }

        .tool-item {
            background: var(--primary-color);
            color: var(--white);
            padding: 8px 20px;
            border-radius: 20px;
            font-size: 0.9rem;
        }

        /* Footer & Contact */
        footer {
            background-color: var(--primary-color);
            color: var(--white);
            padding: 40px 10%;
            text-align: center;
        }

        .social-links a {
            color: var(--white);
            margin: 0 15px;
            text-decoration: none;
            font-size: 1.1rem;
            border: 1px solid var(--white);
            padding: 8px 20px;
            border-radius: 20px;
            transition: 0.3s;
        }

        .social-links a:hover {
            background: var(--white);
            color: var(--primary-color);
        }

        /* Responsive */
        @media (max-width: 768px) {
            header h1 { font-size: 2rem; }
            section { padding: 50px 5%; }
        }
    </style>
</head>
<body>

    <div class="hero-circle"></div>

    <header>
        <h1>عبدالله السلطان [cite: 6]</h1>
        <p>متخصص في التسويق الرقمي وتحليل البيانات </p>
        <p>أجمع بين الانضباط الأكاديمي والمهارات الحديثة لتقديم حلول مبنية على البيانات [cite: 8, 10]</p>
        <div class="btns">
            <a href="#projects" class="btn">مشاهدة أعمالي</a>
            <a href="ملف اعمال عبدالله  Abduallah's portfolio.pdf" class="btn" target="_blank" style="background: transparent; border: 2px solid var(--primary-color); color: var(--primary-color); margin-right: 10px;">تحميل الـ PDF </a>
        </div>
    </header>

    <section id="about">
        <h2>نبذة عني [cite: 7]</h2>
        <div style="text-align: center; max-width: 800px; margin: 0 auto;">
            <p>خريج جامعة المجمعة بمرتبة الشرف الأولى والأول على الدفعة في دبلوم التجارة الإلكترونية بمعدل 4.9[cite: 18, 19, 21, 22].</p>
            <p style="margin-top: 15px;">أركز في عملي على تعزيز اتخاذ القرار وتحسين العائد على الاستثمار عبر برامج Google و McKinsey[cite: 9, 10].</p>
        </div>
    </section>

    <section id="tools" style="background: rgba(125, 110, 93, 0.05);">
        <h2>الأدوات المستخدمة [cite: 24]</h2>
        <div class="tools-list">
            <div class="tool-item">Microsoft Excel [cite: 25]</div>
            <div class="tool-item">Salesforce [cite: 26]</div>
            <div class="tool-item">Canva for Enterprise [cite: 28]</div>
            <div class="tool-item">Google Analytics [cite: 29]</div>
            <div class="tool-item">Semrush & Ahrefs [cite: 30]</div>
        </div>
    </section>

    <section id="projects">
        <h2>المشاريع العملية [cite: 31]</h2>
        <div class="projects-grid">
            <div class="card">
                <h3>مشروع مدينة هاستلتون [cite: 36]</h3>
                <p>حل أزمة ازدحام مروري باستخدام منهجيات McKinsey وخفض زمن التنقل بنسبة 50%[cite: 38, 39].</p>
            </div>
            <div class="card">
                <h3>متجر Nature's Pet [cite: 67]</h3>
                <p>تصميم حملات إعلانية مستهدفة واختبارات A/B Test لزيادة حركة المرور[cite: 74, 77].</p>
            </div>
            <div class="card">
                <h3>تحسين العائد لشركة L'Acier [cite: 88]</h3>
                <p>تحليل بيانات الموقع عبر Excel لتقليل الهدر المالي وتركيز الإنفاق في أوقات الذروة[cite: 92, 93, 137].</p>
            </div>
            <div class="card">
                <h3>إطلاق تطبيق Zone on the Go [cite: 139]</h3>
                <p>حملة بريد إلكتروني (Retention) موجهة للآباء لزيادة معدل التحويل[cite: 151, 154].</p>
            </div>
        </div>
    </section>

    <footer>
        <h2 style="color: white; border-bottom: none;">هل أنت مستعد لتحويل بياناتك إلى قرارات رابحة؟ [cite: 170]</h2>
        <p style="margin-bottom: 30px;">لنتحدث اليوم [cite: 171]</p>
        <div class="social-links">
            <a href="mailto:abduallhsul01@gmail.com">البريد الإلكتروني </a>
            <a href="https://linkedin.com" target="_blank">حساب لينكدإن [cite: 167]</a>
        </div>
        <p style="margin-top: 40px; font-size: 0.8rem; opacity: 0.7;">© 2025 عبدالله السلطان. جميع الحقوق محفوظة.</p>
    </footer>

    <script>
        // تأثير بسيط للتمرير السلس
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });
    </script>
</body>
</html>